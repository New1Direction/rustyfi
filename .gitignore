# Build output
/target

# Cargo lock is kept for binaries (reproducible builds)
# Remove this line if treating as a library-only workspace
# Cargo.lock

# Generated run artifacts (uploads, output, checkpoints)
rustyfi_output/
rustyfi_uploads/
/tmp/rustyfi*/

# Environment / secrets
.env
.env.*
*.pem
*.key

# Benchmark suite scratch space (clones + generated crates + per-run JSON)
bench/.work/

# Editor / OS junk
.DS_Store
.idea/
.vscode/
*.swp
*~
Thumbs.db
